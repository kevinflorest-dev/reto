CREATE TABLE exchange_rate (
                          id    INT AUTO_INCREMENT  PRIMARY KEY,
                          origin_currency    VARCHAR(5) ,
                          value_rate  VARCHAR(5),
                          final_currency   VARCHAR(5),
                          date_rate VARCHAR(5)
                          );
